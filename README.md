# ClaudeCode Changelog Notify

ClaudeCode Changelog Notify is a Cloudflare Worker with a scheduled (cron) trigger that continuously monitors the Claude Code changelog every 15 minutes and sends notifications when new updates are detected.